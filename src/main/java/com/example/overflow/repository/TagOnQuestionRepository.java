package com.example.overflow.repository;

import com.example.overflow.entity.TagOnQuestion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagOnQuestionRepository extends JpaRepository<TagOnQuestion, Integer> {
}
